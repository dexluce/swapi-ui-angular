import { ResolveItemFromUrlPipe } from './resolve-item-from-url.pipe';
import { SwapiService } from 'src/app/services';

describe('ResolveItemFromUrlPipe', () => {
  it('create an instance', () => {
    // const pipe = new ResolveItemFromUrlPipe();
    // expect(pipe).toBeTruthy();
  });
});
