import { GetTypeFromItemPipe } from './get-type-from-item.pipe';

describe('GetTypeFromItemPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTypeFromItemPipe();
    expect(pipe).toBeTruthy();
  });
});
