import { GetIdFromItemPipe } from './get-id-from-item.pipe';

describe('GetIdFromItemPipe', () => {
  it('create an instance', () => {
    const pipe = new GetIdFromItemPipe();
    expect(pipe).toBeTruthy();
  });
});
