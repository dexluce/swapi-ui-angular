import { StarWarsScrollEffectDirective } from './star-wars-scroll-effect.directive';

describe('StarWarsScrollEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new StarWarsScrollEffectDirective();
    expect(directive).toBeTruthy();
  });
});
